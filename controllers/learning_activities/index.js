const create = require('./create');
const show = require('./show');
const update = require('./update');
const showByTeacher = require('./showByTeacher');

module.exports = {
    create, show, update, showByTeacher
}